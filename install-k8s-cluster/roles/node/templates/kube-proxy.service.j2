[Unit]
Description=k8s kube-proxy server
After=network.target
Requires=network.target

[Service]
EnvironmentFile={{k8s_cfg_path}}/kube-proxy.conf
ExecStart={{cmd_path}}/kube-proxy $KUBE_PROXY_ARGS

Restart=on-failure

LimitNOFILE=65535

[Install]
WantedBy=multi-user.target

